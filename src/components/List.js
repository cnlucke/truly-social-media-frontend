import React from 'react'
import { connect } from 'react-redux'

const List = (props) => {
  return (
    <div id="next-list">
      <ul>List</ul>
    </div>
  )
}

export default connect(null, null)(List)
