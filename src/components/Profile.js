import React from 'react'
import { connect } from 'react-redux'

const Profile = (props) => {
  return (
    <div id="next-list">
      <ul>User Profile</ul>
    </div>
  )
}

export default connect(null, null)(Profile)
