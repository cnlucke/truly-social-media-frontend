import React from 'react'
import { connect } from 'react-redux'

const Next = (props) => {
  return (
    <div id="next-list">
      <ul>Next List</ul>
    </div>
  )
}

export default connect(null, null)(Next)
